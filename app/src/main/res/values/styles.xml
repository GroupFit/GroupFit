<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="GroupTextViewStyle">
        <item name="android:layout_marginBottom">3dp</item>
    </style>

    <style name="ButtonDetailStyle" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/colorPrimary</item>

    </style>

    <style parent="Widget.Design.TextInputLayout" name="CustomTextInputLayout">
        <item name="@android:textColor">@android:color/white</item>
    </style>
    <style parent="Widget.AppCompat.EditText" name="CustomEditText">
        <item name="android:colorControlNormal">@android:color/white</item>
        <item name="colorControlActivated">@color/backgroundButtonColor</item>
        <item name="colorControlHighlight">#29B6F6</item>
    </style>

</resources>
